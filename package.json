{
  "name": "gulp-brotli-compression",
  "version": "1.0.0",
  "description": "Compressing assets for static brotli content serving",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^4.0.2"
  },
  "dependencies": {
    "gulp-brotli": "^2.0.2"
  }
}
